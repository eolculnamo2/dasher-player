// This module might just be a big TODO for now and may just get rid of it in favor of hysteresis
// We need to look at relevant trends for
// chunkSize/time ratio,
// some way of inferring buffer direction and severity (?) + infer when starting fresh

export namespace AbrTracking {
  export type T = {};

  export const make = (): T => ({});
}
